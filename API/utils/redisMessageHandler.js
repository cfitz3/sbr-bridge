// MessageFormatter.js

function formatMessage(message) {
  // Add your message formatting logic here
  // For example, you can format the message content
  return message;
}

function formatColouredMessage(colouredMessage) {
  // Add your coloured message formatting logic here
  return colouredMessage;
}

module.exports = {
  formatMessage,
  formatColouredMessage
};